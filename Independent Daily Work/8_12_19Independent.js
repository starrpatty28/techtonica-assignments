// Write a function that takes a word and returns true if the word ends with tonica

function check(word) {
    if(word === [$tonica]) {
        return true;
    } else {
        return false;
    }
}
check(techtonica);

let str1 = "techtonica"


